# APG21A Nix Profile Correction Exit

Phase ID: `APG21A`

## Disposition

Complete — corrected Nix profile retained and APG21 focused audit closed

## Scope and outcome

APG21A reconstructs `nix-language-profile` from APG21's retained defect
evidence, separates structural merge-family breadth from semantic merge risk,
and retains the corrected leaf provisionally. A one-family closed computed
merge is structural Green but semantic Orange; the higher response controls.
Static disjoint local updates remain Green or Yellow, and invariant-bypassing
merge or priority behavior remains Red.

All 15 APG21 shared Nix scenarios, all 21 APG21 Nix scenarios, and all 10
APG21A merge cases pass. Read-only dogfood covers maintained Green through Red,
generated lock, impurity, activation authority, legacy minimal fix, and dynamic
merge behavior. No Nix evaluation, fetch, build, store operation, or activation
occurred.

A focused retained-profile audit reproduced one material PostgreSQL false-
escalation contradiction after the initial Nix candidate. The single permitted
new APG21A correction scopes tested-restore evidence to a consequential change
that relies on restoration as its recovery boundary. The Red stop for
destructive work backed only by an untested backup remains. SQLite and router
behavior remain unchanged, and no second correction is required.

## Resulting repository and distribution

Private development contains 17 canonical leaves, 17 relative projections, 6
stable process rows, 11 provisional rows, and 16 routable non-router
capability-map entries. Nix is a known-unmanaged development skill. The six
v0.2 managed defaults and project, user, release, and map schema version 1
remain unchanged. Public v0.2.0 and active public-backed integration remain
6/6/6.

## Validation and review

Complete documented unit and integration suites, focused Nix and PostgreSQL
tests, skill-library and record-identity checks, capability-map exactness,
Python and shell syntax, executable help, Markdown links, privacy, durable-
reference, mode, schema, whitespace, staged-diff, and fresh non-author reviews
accept the resulting tree. Source and rights review keeps the mutable Nix
2.35.2 manual separate from the Nix 2.35.1 source tag and NixOS/Nixpkgs 26.05
series.

## Boundary

APG21A changes no public repository, active integration, root guidance,
private skill or router, dependency, plugin, release policy, schema, ZUnit
scope, or manager-prompt capability. It performs no application smoke, APG23
work, target-repository mutation, database operation, Nix operation, or public
release.

The current maintainer assignment authorizes APG22 only after APG21A is fully
committed, pushed, and reported. APG22 remains a proposal and read-only dogfood
phase; it does not inherit publication, cutover, decommission, application-
smoke, APG23, or external-mutation authority.
