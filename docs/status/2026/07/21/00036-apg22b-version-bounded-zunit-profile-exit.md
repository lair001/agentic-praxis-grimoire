# APG22B Version-Bounded ZUnit Profile Exit

Phase ID: `APG22B`

## Disposition

Complete — version-bounded ZUnit profile implemented

## Scope and outcome

APG22B retains `zunit-test-profile` provisionally for exactly ZUnit v0.8.2
with Zsh 5.9.2. The exact pair passes 98 tagged upstream tests, seven APG
focused tests, expected risky, assertion-failure, and invalid-discovery cases,
owned process and lifecycle checks, and no-residue cleanup in an unprivileged
disposable source-build environment.

ZUnit v0.8.2 with Zsh 5.3.1 is unsupported on the tested environment because
the runner's Revolver dependency probe times out. No upstream correction,
adjacent-version inference, current-stable inference, or unbounded version
claim is made. Every other pair stops pending fresh disposable evidence.

## Resulting repository and distribution

Private development contains 19 canonical leaves, 19 relative projections, 19
catalog rows, 6 stable process rows, 13 provisional rows, and 18 routable non-
router capability-map entries. The new profile is known unmanaged by the
project-local lifecycle. Schema version 1 and the six managed v0.2 project,
user, public-release, and active public-backed integration skills remain
unchanged.

ADR 0014 now records the subsequent version-bounded retention disposition.
The APG19, APG19A, and APG22 deferrals remain truthful historical decisions and
point to this later evidence rather than being rewritten.

## Validation and review

The complete documented unit and integration suites, development and public
text/JSON checkers, record identity, Python compilation, applicable Bash and
POSIX-shell syntax, exact-runner ZUnit DSL validation, command help, changed
Markdown links, privacy, durable-reference, mode, schema, whitespace, staged-
diff, and fresh non-author compatibility, semantic, threshold, integration,
documentation, and complete-diff reviews
accept the resulting tree.

The frozen matrix has 48/48 passing dispositions for the claimed exact pair
and unsupported-runtime stops. Candidate correction count and detailed review
findings are recorded in publication-excluded evidence.

## Boundary and next readiness

APG22B performs no root-guidance removal, private-skill or personal-router
decommission, generic shell-test or writing-skill implementation, public
release, active-integration mutation, dependency installation, global ZUnit or
Zsh installation, Nix operation, application smoke, or APG23 work. No tracked
record uses an internal commit hash as phase or state identity.

The original v0.3 manager-assignment and ZUnit scope objectives are terminal.
APG23 is semantically ready for separate human authorization, but it has not
begun. Pre-release application smoke remains deferred to APG23 and APG24.

## Subsequent APG22C disposition

APG22C invalidated this phase's original startup-isolation evidence because
the sentinel was installed outside the startup directory selected through
`ZDOTDIR`. The corrected harness requires an unsuppressed positive control to
load the sentinel, requires `-f` to suppress it, and requires the focused ZUnit
test process to observe its absence.

The corrected exact matrix retains the 5.9.2 support boundary and the 5.3.1
unsupported result. No ZUnit skill correction, maturity change, distribution
change, application smoke, or successor work occurs. Exit 00037 owns the
forward correction and the post-correction readiness boundary.
